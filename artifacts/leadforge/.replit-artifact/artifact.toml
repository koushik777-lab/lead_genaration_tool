kind = "web"
previewPath = "/"
title = "LeadForge Elite"
version = "1.0.0"
id = "artifacts/leadforge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18555

[services.development]
run = "pnpm --filter @workspace/leadforge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/leadforge", "run", "build" ]
publicDir = "artifacts/leadforge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18555"
BASE_PATH = "/"
